This directory contains some simple example of usage of the CAN8 interface for GPIO and CAN Bus on the Zedboard platform
  e-stop:   connect 1 input to emergency stop button.
  encoder:  connect 3 inputs to encoder for jog (A-B and axis select).
  spindle:  generate PWM on output pin controlled by encoder.
  halrun:   example of simple halrun usage.
  dm-style: a complete 3 axis machine configuration.
